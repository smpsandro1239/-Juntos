# +JUNTOS
# Ficheiro: apps/mobile/pubspec.yaml
# Descrição: Dependências e metadados do projeto Flutter
# Autor: (+JUNTOS team)
# Locale: pt_PT

name: mobile
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Core
  riverpod: ^2.5.1
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5 # Adicionado
  go_router: ^13.2.0
  dio: ^5.4.1
  intl: ^0.20.2

  # Database
  drift: ^2.15.0
  sqlite3_flutter_libs: ^0.5.20
  path_provider: ^2.1.2
  path: ^1.9.0

  # UI
  mapbox_gl: ^0.20.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  build_runner: ^2.4.8
  drift_dev: ^2.15.0
  riverpod_generator: ^2.4.0

flutter:
  uses-material-design: true
  generate: true
