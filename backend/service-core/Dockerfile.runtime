FROM eclipse-temurin:21-jre

WORKDIR /app

# Copy the pre-built fat/boot JAR into the image. Replace the JAR name if your build produces a different filename.
ARG JAR_FILE=build/libs/service-core-0.0.1-SNAPSHOT.jar
COPY ${JAR_FILE} app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "/app/app.jar"]
